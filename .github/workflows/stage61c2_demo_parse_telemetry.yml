name: Stage61C2 Demo Parse Telemetry
on:
  workflow_dispatch:
jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m py_compile app/stage61c2_demo_ea_telemetry_validator.py
