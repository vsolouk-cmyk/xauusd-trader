# Stage23C Patch

This patch adds a locked validation module for the Stage23B promotion-review candidates. It does not modify Stage18A v2.

## Copy

```bash
cd ~/Desktop/xauusd-trader
unzip -o ~/Downloads/xauusd_stage23c_promotion_candidate_validation_patch.zip -d .
```

## Run

```bash
cd ~/Desktop/xauusd-trader
python3 -m app.stage23c_promotion_candidate_validation
cat data/reports/stage23c_promotion_candidate_validation/stage23c_promotion_candidate_validation.md
```
