# Stage24A Patch

Adds `app/stage24a_remaining_behavior_discovery.py` and documentation.

Copy into the repo with:

```bash
cd ~/Desktop/xauusd-trader
unzip -o ~/Downloads/xauusd_stage24a_remaining_behavior_discovery_patch.zip -d .
```

Run with:

```bash
python3 -m app.stage24a_remaining_behavior_discovery
cat data/reports/stage24a_remaining_behavior_discovery/stage24a_remaining_behavior_discovery.md
```
