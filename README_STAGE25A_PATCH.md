# Stage25A Patch

This patch adds:

```text
app/stage25a_regime_filter_discovery.py
docs/STAGE25A_REGIME_FILTER_DISCOVERY.md
```

Run:

```bash
cd ~/Desktop/xauusd-trader
python3 -m app.stage25a_regime_filter_discovery
cat data/reports/stage25a_regime_filter_discovery/stage25a_regime_filter_discovery.md
```

Safe git commands:

```bash
cd ~/Desktop/xauusd-trader
git add -A
git commit -m "Add Stage25A regime filter discovery"
git pull --rebase
git push
```
