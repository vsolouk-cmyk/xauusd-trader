# Stage25C Patch

Files:

```text
app/stage25c_deduped_filter_validation.py
docs/STAGE25C_DEDUPED_FILTER_VALIDATION.md
```

Copy/unzip:

```bash
cd ~/Desktop/xauusd-trader
unzip -o ~/Downloads/xauusd_stage25c_deduped_filter_validation_patch.zip -d .
```

Run:

```bash
cd ~/Desktop/xauusd-trader
python3 -m app.stage25c_deduped_filter_validation
cat data/reports/stage25c_deduped_filter_validation/stage25c_deduped_filter_validation.md
```
