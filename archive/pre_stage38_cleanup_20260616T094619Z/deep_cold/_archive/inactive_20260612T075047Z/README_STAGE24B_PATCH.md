# Stage24B Patch

Copy into `~/Desktop/xauusd-trader` with:

```bash
cd ~/Desktop/xauusd-trader
unzip -o ~/Downloads/xauusd_stage24b_remaining_behavior_discovery_patch.zip -d .
```

Run:

```bash
cd ~/Desktop/xauusd-trader
python3 -m app.stage24b_remaining_behavior_discovery
cat data/reports/stage24b_remaining_behavior_discovery/stage24b_remaining_behavior_discovery.md
```

This patch is research/shadow-only and does not modify Stage18A v2 or Stage23D.
