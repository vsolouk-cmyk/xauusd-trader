project:
  name: xauusd-trader
  symbol: XAUUSD
  phase: stage_0_data_source_decision
  timezone_for_reports: Asia/Tehran

rules:
  baseline_first: true
  data_quality_first: true
  ml_allowed_before_baseline: false
  paper_order_allowed_before_forward_shadow: false
  live_allowed: false

paths:
  raw_data: data/raw
  normalized_data: data/normalized
  reports: data/reports
  logs: data/logs

current_status:
  data_source_selected: false
  collector_enabled: false
  baseline_lab_enabled: false
  ml_enabled: false
  forward_shadow_enabled: false
  paper_order_enabled: false
