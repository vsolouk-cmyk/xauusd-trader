# Stage 3A second-source validation configuration.
# Goal: validate the fixed candidate on an independent/broker-like data source.
#
# This stage is NOT ML.
# This stage is NOT paper-order.
# This stage is NOT live trading.

candidate:
  family: sma_trend_1h
  variant: sma10_h12_dist10_cool1
  interval: 1h
  sma_window: 10
  horizon_bars: 12
  min_distance_usd: 10.0
  cooldown_bars: 1

cost_model:
  assumed_spread_usd: 0.30
  assumed_slippage_usd: 0.05
  total_roundtrip_cost_usd: 0.35

sources:
  primary:
    name: twelvedata_primary
    db_path: data/store/xauusd.sqlite
    required: true

  secondary:
    name: second_source
    db_path: data/second_source/second_source.sqlite
    required: false

decision:
  min_secondary_trades: 100
  min_secondary_profit_factor: 1.05
  require_secondary_total_positive: true
  require_secondary_cost_x4_positive: true
  max_total_net_degradation_ratio: 0.65
  require_long_short_not_both_negative: true

report_dir: data/reports
