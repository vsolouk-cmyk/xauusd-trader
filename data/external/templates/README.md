# External context CSV templates

These are schema templates only. Do not treat them as data. Populate real external context files under `data/external/` or `data/reference/` using the accepted paths in the Stage45B1 report.
